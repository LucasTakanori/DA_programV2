Seguidamente, se encuentra una lista con ejemplos para los métodos de aumentación, hay un conjunto de métodos en los que las aumentaciones no afectan mucho al reconocimiento de la voz, por tanto, se les aplica la etiqueta "FACIL" estos métodos son:

PITCH_SHIFTING
TIME_STRETCHING
VTLP

Para los demás métodos hay un ejemplo de dificultad media y difícil, en el archivo ejemplos.tsv se puede ver todos los archivos y sus aumentaciones. La lista de los ejemplos con 2 dificultades es:

CLIPPING
FREQUENCY_MASKING
MP3_COMPRESSION
WHITE_NOISE
SPLICE_OUT
